h4 Socket.IO
div
  form(name="socketForm")
    input.form-control.mgb1(type="text" ng-model="message", required)
    button.btn.btn-primary(ng-click="broadcast()" ng-disabled="!socketForm.$valid") 전파하기 (Broadcast)

  ul
    li.list-unstyled(ng-repeat="msg in messages track by $index") {{$index+1}}. {{msg}}